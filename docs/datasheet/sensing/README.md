# Sensing Datasheets

Examples:
- TCS3200 color sensor module docs
- ToF or distance sensor module docs as candidate size sensing references
- IR break-beam module specs as candidate size sensing references
- Microswitch docs only if a states mechanism needs them

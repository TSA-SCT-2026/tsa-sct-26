# Run Logs

Calibration and validation runs use CSV serial output.

## Format

Store run data as:

```text
docs/runs/YYYYMMDD_label.csv
```

Each row should include timestamp, sensor readings, computed classification, chute target, and final bin result.

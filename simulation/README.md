# Simulation

Browser model for the sorter control flow.

## Run

```sh
cd simulation
npm install
npm test
./build.sh
open simulator.html
```

## What It Models

- brick feed timing
- size and color classification state
- chute route selection
- bin counts
- basic error states

It is a control-flow simulator, not a physics model.
